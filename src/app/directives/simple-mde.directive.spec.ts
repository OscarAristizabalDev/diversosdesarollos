import { SimpleMdeDirective } from './simple-mde.directive';

describe('SimpleMdeDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleMdeDirective();
    expect(directive).toBeTruthy();
  });
});
